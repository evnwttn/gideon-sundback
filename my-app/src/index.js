import React from "react";
import ReactDOM from "react-dom";
import "./index.css";

export default function GideonSundback() {
  return <>Sometimes it do be like that.</>;
}

ReactDOM.render(GideonSundback(), document.getElementById("root"));
